const slides=[...document.querySelectorAll('.slide')],dots=document.querySelector('#dots'),counter=document.querySelector('#counter');let current=0;
slides.forEach((slide,i)=>{const dot=document.createElement('i');dot.title=slide.dataset.label;dot.onclick=()=>go(i);dots.append(dot)});
function go(next){current=(next+slides.length)%slides.length;slides.forEach((slide,i)=>slide.classList.toggle('active',i===current));[...dots.children].forEach((dot,i)=>dot.classList.toggle('on',i===current));counter.textContent=`${String(current+1).padStart(2,'0')} / ${String(slides.length).padStart(2,'0')}`;history.replaceState(null,'',`#${current+1}`)}
document.querySelector('#next').onclick=()=>go(current+1);document.querySelector('#prev').onclick=()=>go(current-1);window.addEventListener('keydown',event=>{if(['ArrowRight','PageDown',' '].includes(event.key)){event.preventDefault();go(current+1)}if(['ArrowLeft','PageUp'].includes(event.key)){event.preventDefault();go(current-1)}if(event.key==='Home')go(0);if(event.key==='End')go(slides.length-1)});go(Math.max(0,Number(location.hash.slice(1)||1)-1));
const stats=[['131 M','palabras para descubrir'],['1.385','obras y documentos'],['12','bibliotecas especializadas'],['27','colecciones conectadas']];let stat=0;setInterval(()=>{stat=(stat+1)%stats.length;const value=document.querySelector('#stat-value'),label=document.querySelector('#stat-label');value.animate([{opacity:.15,transform:'scale(.84)'},{opacity:1,transform:'scale(1)'}],{duration:520,easing:'cubic-bezier(.2,.8,.2,1)'});value.textContent=stats[stat][0];label.textContent=stats[stat][1]},2400);
document.querySelector('#tour').onclick=event=>{const routes=['../#/biblioteca','../#/preguntar','../#/rezar','../#/discover'],frame=document.querySelector('#tour-frame');let at=0;event.currentTarget.textContent='Recorriendo Atlas…';frame.src=routes[at];const timer=setInterval(()=>{at++;if(at===routes.length){clearInterval(timer);event.currentTarget.textContent='▶ Ver recorrido';return}frame.src=routes[at]},1800)};document.querySelector('#whatsapp').onclick=()=>window.open(`https://wa.me/?text=${encodeURIComponent(`Conoce Atlas 1.0: una biblioteca viva para encontrar, preguntar y rezar.\n${location.href}`)}`,'_blank','noopener');
